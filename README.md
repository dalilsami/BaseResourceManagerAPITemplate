# Resource API Template (WIP)

This API lets you manage resources from your MongoDB database.

The goal of this API's design is that all the resource controllers are reachable within any resource controller.
